<div class="uk-section uk-section-light" id="team">
    <div class="uk-container uk-text-center">
        <h2 class="uk-heading-mini text-light uk-margin-medium-bottom" uk-scrollspy="cls: uk-animation-fade; repeat: true">Our Team</h2>

        {# <div class="uk-grid-large" uk-grid uk-scrollspy="target: > div; cls: uk-animation-scale-up; delay:150; repeat: true">
            {% for member in data.team %}
                <div class="uk-width-1-3 uk-width-1-5@m team-member">
                    <img uk-img="data-src: assets/images/avatar.jpg" alt="{{member.name}}" class="uk-border-circle">
                    <div class="uk-text-center uk-margin-small-top">
                        <div class="text-bold member-name">{{member.name}}</div>
                        <div class="text-light uk-text-small member-designation">{{member.designation}}</div>
                        {% if member.department %}
                            <div class="text-light uk-text-small member-department">{{member.department}}</div>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div> #}

        <div uk-slider="sets: true; finite: true">
            <ul class="uk-slider-items uk-child-width-1-2 uk-child-width-1-5@m uk-grid">
                {% for member in data.team %}
                    <li class="team-member">
                        <img uk-img="data-src: {{data.assets.images}}/team/{{member.image}}" alt="{{member.name}}" class="uk-border-circle">
                        <div class="uk-text-center uk-margin-small-top">
                            <div class="text-bold member-name">{{member.name}}</div>
                            <div class="text-light uk-text-small member-designation">{{member.designation}}</div>
                            {% if member.department %}
                                <div class="text-light uk-text-small member-department">{{member.department}}</div>
                            {% endif %}
                        </div>
                    </li>
                {% endfor %}
            </ul>

            <ul class="uk-slider-nav uk-dotnav uk-flex-center uk-margin"></ul>

        </div>

        {# <div class="uk-padding-small uk-margin-large-top">
            <div class="uk-button uk-button-primary">Join Us</div>
        </div> #}
    </div>
</div>